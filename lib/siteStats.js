// The reach figures SciTeens publishes, shared by the home hero band and
// the donation appeal so the two can never quote different numbers.
// These are reported counts, not estimates: update them together with
// whatever the team publishes, and never round one up to fill a layout.
export const SITE_STATS = [
  { value: 400, suffix: '+', label: 'stats.monthly_users' },
  { value: 50, suffix: '', label: 'stats.schools' },
  { value: 50, suffix: '+', label: 'stats.countries' },
]
