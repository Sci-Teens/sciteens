# Loaded into process.env for every deployed Cloud Function (Firebase CLI
# dotenv support). Non-secret values only — MEILI_MASTER_KEY is Secret
# Manager (see the defineSecret('MEILI_MASTER_KEY') call in index.js), never
# here.
MEILI_HOST=https://meilisearch-zjmirht5bq-ue.a.run.app
