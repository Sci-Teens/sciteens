# dependencies
node_modules

# next.js
.next
# `next build` output for the two Playwright dev servers
# (playwright.config.js#webServer sets NEXT_DIST_DIR)
.next-e2e-emulator
.next-e2e-live
out

# playwright run output
test-results
playwright-report

# production
build
coverage

# vercel / firebase
.vercel

# lockfile — pnpm's format is not prettier-compatible
pnpm-lock.yaml

# next-sitemap build output
public/robots.txt
public/sitemap.xml

# untracked migration tracker — hand-maintained, not committed
MIGRATION-PHASES.md

# pdfjs-dist worker, copied verbatim from node_modules — vendor code,
# never hand-edited (see scripts/copy-pdf-worker.js)
public/pdf.worker.min.mjs

# generated body-text search corpus, minified JSON by design
# (scripts/build-search-index.mjs)
public/content/article-search.json
